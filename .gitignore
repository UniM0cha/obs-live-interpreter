# 비밀/자격증명 — 절대 커밋 금지
.env
.env.*
!.env.example
*.key
*credentials*
*secret*

# 빌드 산출물 (C++ / OBS 플러그인)
build/
cmake-build-*/
*.o
*.obj
*.so
*.dylib
*.dll
*.plugin/

# Python 프로토타입
__pycache__/
*.pyc
.venv/
venv/
.python-version

# 프로토타입 — git 미포함 (로컬 파이프라인 검증용; 설교자 실명 등이 들어가므로 추적 안 함)
prototype/

# 출력 오디오 (커밋 금지)
server/out*.wav
*.aiff

# OS / 에디터
.DS_Store
.idea/
.vscode/
*.swp
